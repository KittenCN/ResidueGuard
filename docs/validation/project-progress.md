# 全项目阶段进度与放行门槛

更新：2026-09-20。用户已授权跨阶段开发；系统修改仍须满足原有安全门槛。此页区分已实现模块、纯逻辑测试与真实平台验收，不将 `notRun` 计为通过。最新命令和数字见 [本轮综合报告](final-development-results-2026-09-20.md)，此前结果保留在 [P0/P1](test-results-2026-09-20.md) 和 [P2](p2-results-2026-09-20.md) 历史报告。

| 阶段 | 已实现/已有证据 | 剩余必需工作 | 当前结论 |
|---|---|---|---|
| P0 | 实机 macOS/Xcode/Swift/SDK、公开手册/SDK 与隔离实验计划；只读源探针 | 隔离 app、精确权限与 helper 生命周期/签名平台实验 | 环境已核验，写能力实验 `notRun` |
| P1 | 原生 SwiftUI App、独立 Swift 核心、分类/搜索/详情、合成演示、dry-run 独立确认 | 人工浅深色/高对比度/VoiceOver/最小窗口；最终总计 11 项 UI 回归通过，细节见综合报告 | 开发基线可用；不等于完整可访问性验收 |
| P2 | 有界 no-follow 只读启动配置/顶层应用索引、失败来源行、coverage/provenance、取消、差异比较；显式目录授权 UI；静态代码签名元数据读取 | sandbox 目录授权/拒绝端到端人工验证、完整签名归属/共享 owner 图、运行时关联、BTM/登录项真实 profile、完整误判矩阵 | 受限只读来源可用；未完成全来源 P2 |
| P3 | 不可变事务计划、独立确认契约、备份/日志顺序协调、失败停止、恢复冲突策略；10 项合成测试纳入核心测试 | 真实 driver、授权、持久化备份/隔离/journal、TOCTOU 执行边界、服务卸载、崩溃补偿和隔离 M01–M12 | 纯策略已实现；生产 gate 关闭，无真实清理 |
| P4 | 独立 ResidueSecurity：版本化有限 DTO、精确调用者策略、绑定 token/nonce/期限、重放拒绝；9 项策略测试 | 真实 XPC 身份传输与握手、helper 可执行/安装/批准/升级/移除、root 文件操作、持久化执行日志及攻击矩阵 | 策略已实现；未实现生产 helper |
| P5 | 19 类权限注册表、独立机制/引导、历史快照与影响扩大门禁；未知 build 禁止读写 | 合格旧版只读 adapter（可选）、真实精确 reset 和后置验证、权限与应用真实关联 | 现代引导可用；TCC 直读及 reset 关闭 |
| P6 | 默认脱敏 JSON/CSV 报告与 GUI 预览、公式注入防护、10,000 条核心报告测试、本地预览打包通过；发布检查实际拒绝 Debug 包 | 实际 GUI 性能/可访问性、日志留存/忽略规则、多 OS/Intel、Developer ID/Hardened Runtime、公证、干净安装/升级/卸载 | 发布准备部分实现，不是正式发布 |

## 当前验证汇总

Core 30、Security 11、Platform 28 项已实际通过（最新路径/schema/token 回归见 VM 接续报告）；Core 的 10,000 条合成报告处理观测为 0.437242541 秒（JSON + CSV），只测核心报告生成，不是 GUI 滚动或扫描性能。最终总计 11 项 GUI 测试首次存在报告预览辅助功能断言失败；显式提供 accessibilityValue 并修正读取断言后，针对性 1 项复测通过，最终完整 11 项通过（94.763 秒，0 失败）。静态签名元数据不是签名有效性/系统信任验证。

## 仍然关闭的能力

没有真实清理 driver、提权 helper、服务卸载/隔离、备份恢复、权限重置、全局 reset 或 macOS 27+ TCC 直读。真实扫描行不可执行；合成 dry-run 完成不会生成系统写授权。隔离实验及分发 profile 没有通过，不因纯逻辑测试开放 mutation。

现已识别用户安装的 VirtualBuddy VM，并建立停止状态的回滚副本和专用交换目录。客体重启后待本地登录，ISO-01/04 仍未执行。宿主可用 codesigning identity 计数复查仍为 0；ad-hoc 不替代 helper/Developer ID 信任。最新证据见 [VM 接续](vm-continuation-2026-09-20.md)。

## 下一阶段优先级

新增 UI 回归已通过，下一步完成 sandbox 目录授权/拒绝的真实验证；继续补齐 P2 身份/归属/runtime/BTM 的可审计只读 profile。P3/P4 必须先具备可回滚测试环境与合适签名，再实现真实 driver/helper 并运行 M/S 系列平台实验。P6 分发须另有 Developer ID、公证与干净环境证据。

全项目仍未完成。必须让每个已开放动作对应真实 OS/build 证据，且无未解决的错误删除或越权路径，才能通过清理与发布验收。平台不支持的权限可明确 guidedOnly，但未实现或未测试的模块不能冒充平台限制。

本轮 GUI 完整回归最终 11 项、0 失败（99.134 秒），首次过滤断言失败和测试状态同步改进记录于 [VM 接续报告](vm-continuation-2026-09-20.md)。上文旧综合报告数字属于前一开发轮次。
