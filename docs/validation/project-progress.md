# 全项目阶段进度与放行门槛

更新：2026-09-20。最新动态以 [当前状态](status.md) 和 [本地开发范围](local-development-scope.md) 为准；Developer ID/公证发布暂不在本轮目标。用户已授权跨阶段开发；系统修改仍须满足原有安全门槛。此页区分已实现模块、纯逻辑测试与真实平台验收，不将 `notRun` 计为通过。最新命令和数字见 [本轮综合报告](final-development-results-2026-09-20.md)，此前结果保留在 [P0/P1](test-results-2026-09-20.md) 和 [P2](p2-results-2026-09-20.md) 历史报告。

| 阶段 | 已实现/已有证据 | 剩余必需工作 | 当前结论 |
|---|---|---|---|
| P0 | 实机 macOS/Xcode/Swift/SDK、公开手册/SDK 与隔离实验计划；只读源探针 | 隔离 app、精确权限与 helper 生命周期/签名平台实验 | 环境已核验；自有 VM 正常路径子集通过，完整矩阵未完成 |
| P1 | 原生 SwiftUI App、独立 Swift 核心、分类/搜索/详情、合成演示、dry-run 独立确认 | 人工浅深色/高对比度/VoiceOver/最小窗口；最新全量22项UI：21通过、1显式跳过，详见当前状态 | 开发基线可用；不等于完整可访问性验收 |
| P2 | 有界 no-follow 只读启动配置/顶层应用索引、失败来源行、coverage/provenance、取消、覆盖范围约束快照比较和已授权根局部复扫；显式目录授权 UI；静态代码签名元数据读取；有界候选归属图/多实例GUI | 完整签名归属/共享 owner 图、运行时关联、BTM/登录项真实 profile、完整误判矩阵 | 受限只读来源可用；未完成全来源 P2 |
| P3 | 不可变事务计划、独立确认契约、备份/日志顺序协调、失败停止、恢复冲突策略；独立持久日志、固定VM隔离/恢复及单次bootout、独立只读审计；79项Quarantine测试含文件/日志进程崩溃 | 真实 driver、授权、持久化备份/隔离集成、TOCTOU 执行边界、服务卸载、崩溃补偿和隔离 M01–M12 | 策略与隔离实验分层实现；生产 gate 关闭 |
| P4 | 独立 ResidueSecurity：版本化有限 DTO、精确调用者策略、绑定 token/nonce/期限、重放拒绝；23项策略及有界wire测试；独立XPC实验与生产信任分离 | 生产信任根与 XPC 协议集成、helper 可执行/安装/批准/升级/移除、root 文件操作、持久化执行日志及攻击矩阵 | 策略已实现；未实现生产 helper |
| P5 | 19 类权限注册表、独立机制/引导、历史快照与影响扩大门禁；未知 build 禁止读写 | 合格旧版只读 adapter（可选）、真实精确 reset 和后置验证、权限与应用真实关联 | 现代引导可用；TCC 直读及 reset 关闭 |
| P6 | 默认脱敏 JSON/CSV 报告与 GUI 预览、公式注入防护、10,000 条核心报告测试、本地预览打包通过；保留规则/私有配置已实现并有VM正常路径；发布检查实际拒绝 Debug 包 | 实际 GUI 性能/可访问性、日志留存、多 OS/Intel、Developer ID/Hardened Runtime、公证、干净安装/升级/卸载 | 发布准备部分实现，不是正式发布 |

## 当前验证汇总

最新数字、失败/跳过和真实 VM 证据统一见[当前状态](status.md)。表格中早期阶段的计数只描述原始阶段验收，不覆盖后续新增包。新增 SQLite 崩溃恢复、备份、隔离原型、恢复核验、历史导入及真实 XPC 无特权实验已形成独立证据；它们尚未拼成生产清理事务。

## 仍然关闭的能力

产品未开放真实清理 driver、提权 helper、服务卸载/隔离、备份恢复、权限重置、全局 reset 或 macOS 27+ TCC 直读。真实扫描行不可执行；合成 dry-run 完成不会生成系统写授权。隔离实验及分发 profile 没有通过，不因纯逻辑测试开放 mutation。

VirtualBuddy VM 已完成回滚副本启动验证、ISO-01/04 正常路径子集和真实 GUI 目录扫描。SQLite 日志含独立进程终止测试，备份模块和精确运行状态采集继续集成。完整 M/S 故障矩阵仍未通过；ad-hoc 不自动证明特权 peer 信任。最新证据见 [当前状态](status.md)。

## 下一阶段优先级

真实沙箱目录选择与扫描已验证；继续补齐 P2 身份/归属/runtime/BTM 的可审计只读 profile。P3/P4 必须先具备可回滚测试环境与合适签名，再实现真实 driver/helper 并运行 M/S 系列平台实验。P6 公开分发另行安排，本轮不要求 Developer ID/公证。

全项目仍未完成。必须让每个已开放动作对应真实 OS/build 证据，且无未解决的错误删除或越权路径，才能通过清理与发布验收。平台不支持的权限可明确 guidedOnly，但未实现或未测试的模块不能冒充平台限制。
